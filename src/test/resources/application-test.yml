spring.cloud:
  config:
    server:
      git:
        uri: http://toto.africa.com
encrypt:
  keyStore:
    location: classpath:/server.jks
    password: changeme
    alias: mytestkey
    secret: changeme